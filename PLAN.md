# 나의 AI 투자 매니저 — 기획서 v0.1

> 작성일: 2026-07-19 · 개인용 우선 → 추후 서비스화 · 매매는 시뮬레이션만 · 채널: 텔레그램 + 웹 대시보드

## 1. 제품 비전

**"진짜 투자기관 리서치센터가 나 한 명을 위해 일하는 것처럼."**

- 주요 시장: **한국(KOSPI/KOSDAQ) + 미국(NYSE/NASDAQ)**
- 장시작/마감 정시에 기관급 리포트를 텔레그램으로 발송
- 다양한 투자전략을 가상 포트폴리오로 병렬 시뮬레이션
- 모든 권고에 **근거와 확률**이 붙고, 대화형으로 질문 가능

## 2. 요구사항 → 레퍼런스 매핑

> **코드 참고 정책 (2026-07-19 확정)**: 현재는 **개인 스터디 단계 → 7개 리포 전부 코드 레벨 참고 OK** (AGPL은 개인 사용 자유, Fincept 상용 라이선스도 personal use/individual learning 명시 허용). 단, AGPL 계열(prism-insight, FinceptTerminal)에서 빌린 코드는 §9의 출처 추적 규칙에 따라 기록 — 서비스화(§10) 시점에 해당 부분만 재작성하면 되도록.

| 요구사항 | 차용 대상 | 라이선스 | 개인 단계 | 서비스화 시 |
|---|---|---|---|---|
| 정시 리포트 발송 | prism-insight (오케스트레이터+cron 구조) | AGPL ⚠️ | 코드 참고 OK | **재작성 필요** |
| 멀티에이전트 분석/판단 | TradingAgents (토론 파이프라인) | Apache 2.0 | 코드 차용/포크 | 그대로 사용 가능 |
| 전략 시뮬레이션/백테스트 | Vibe-Trading (엔진 설계, Alpha Zoo) | MIT | 코드 차용 | 그대로 사용 가능 |
| 한국 데이터 (시세/공시/수급) | cluefin (KIS/키움/DART SDK) | MIT | 라이브러리로 직접 사용 | 그대로 사용 가능 |
| 미국 데이터 | TradingAgents dataflows (yfinance/FRED/EDGAR) | Apache 2.0 | 코드 차용 | 그대로 사용 가능 |
| 소셜/카피 기능(장래) | AI-Trader | MIT 표기(파일 없음) | 기획 참고 | 코드 재사용은 LICENSE 확인 후 |
| KIS API 공식 스펙 (KR+US 시세·실시간) | open-trading-api (한투 공식) | 명시 라이선스 없음(고객용 참고 코드) | auth/WS 코어 래핑 | 자체 구현 유지(재배포 금지) |
| 터미널 UX·시뮬레이션 대회 설계 | FinceptTerminal | AGPL+상용(비즈니스 이용 금지)⚠️ | 코드 참고 OK (개인 학습 허용) | **클린룸 재작성 필요** |

## 3. 시스템 아키텍처 (5-레이어)

```
┌─────────────────────────────────────────────────────────┐
│  ⑤ 딜리버리     텔레그램 봇(방송+Q&A) · 웹 대시보드(Next.js) │
├─────────────────────────────────────────────────────────┤
│  ④ 스케줄러     APScheduler: KR 08:30/16:00 · US 22:30/06:30 KST │
├─────────────────────────────────────────────────────────┤
│  ③ 두뇌(멀티에이전트) — TradingAgents 구조 차용            │
│     애널리스트팀: 기술 · 펀더멘털 · 뉴스 · 수급(외인/기관)   │
│     매크로: 시장 레짐 판정 (5단계)                          │
│     Bull vs Bear 토론 → 리서치 판정 → 확률·근거 구조화 출력  │
├─────────────────────────────────────────────────────────┤
│  ② 시뮬레이션    전략 프리셋 N개 × 가상 포트폴리오 병렬 운용   │
│     성과추적: 수익률·MDD·Sharpe·승률 / 백테스트 검증         │
├─────────────────────────────────────────────────────────┤
│  ① 데이터        KR: cluefin(KIS 시세·DART) + pykrx(수급)    │
│                 US: yfinance + FRED + SEC EDGAR            │
│                 뉴스: 웹검색 API / 네이버금융                │
│                 저장: SQLite (리포트 아카이브 + FTS5 + 임베딩) │
└─────────────────────────────────────────────────────────┘
```

핵심 설계 원칙:
1. **판단 로그 = Q&A의 원천.** 모든 에이전트 판단(근거·확률·데이터 스냅샷)을 구조화 저장 → "왜 추천했어?"에 원본 근거로 답변
2. **비용 최적화**: 딥씽킹(판정자) / 퀵씽킹(애널리스트) 모델 분리 — TradingAgents 패턴
3. **룩어헤드/환각 방지**: 검증된 시장 스냅샷 강제 주입, 날짜 필터링 — TradingAgents 패턴
4. **반성 루프**: 실현 수익률을 다음 판단 프롬프트에 주입 — prism-insight 저널 개념 (자체 구현)

## 4. 리포트 설계 (기관 스타일)

### 알람 스케줄 (KST)
| 시각 | 리포트 | 내용 |
|---|---|---|
| 08:30 | 🇰🇷 장전 브리핑 | 간밤 미국 증시·환율, 주요 뉴스/공시, 오늘의 이벤트(실적·청약), 주목 종목 3-5개(근거+확률), 시뮬 포트폴리오 오늘의 계획 |
| 16:00 | 🇰🇷 마감 브리핑 | 지수 마감·수급(외인/기관/개인), 특징주 리뷰, 시뮬 포트폴리오 성과(일간/누적), 내일 관전포인트 |
| 22:30 | 🇺🇸 장전 브리핑 | 프리마켓 동향, 주요 실적/경제지표 일정, 주목 종목 |
| 06:30 | 🇺🇸 마감 브리핑 | 지수 마감, 섹터 히트맵, 특징주, 시뮬 성과 |
| 일요일 | 📊 주간 리포트 | 주간 성과 비교(전략별), 다음 주 캘린더, 레짐 판단 변화 |

### 주목 종목 카드 포맷 (권고근거 + 확률)
```
📌 삼성전자 (005930) — 매수 후보 [확신도 72%]
├ 판정: BUY (Bull 우세 — 토론 3라운드)
├ 근거: ① 외인 5일 연속 순매수 +3,200억 ② HBM 수주 뉴스 모멘텀
│       ③ RSI 58 — 과열 아님 ④ 반도체 섹터 레짐: strong_bull
├ 리스크: 환율 1,400원 돌파 시 외인 이탈 가능 (Bear 논점)
├ 시나리오: 진입 71,000 / 목표 78,000 (+9.8%) / 손절 67,500 (-4.9%)
└ 근거 상세: /why 005930
```

## 5. 전략 시뮬레이션 설계

각 전략 = 독립 가상 포트폴리오 (초기 자금 각 1억 KRW / $100k):

| 전략 | 방식 | 참고 |
|---|---|---|
| AI 종합판단 | 멀티에이전트 토론 최종 판정 그대로 | TradingAgents |
| 모멘텀 | 수급+거래량 서지 스크리닝 | prism-insight trigger 개념 |
| CAN SLIM | O'Neil 룰 기반 | prism-insight 프롬프트 개념 |
| 가치투자 | PER/PBR/ROE 스크리닝 + LLM 정성평가 | cluefin DART 재무 |
| 퀀트 팩터 | Alpha Zoo 상위 알파 조합 | Vibe-Trading |
| 벤치마크 | KOSPI200/S&P500 바이앤홀드 | 비교 기준 |

- 규칙: 슬롯 최대 10개, 종목당 10%, 섹터 최대 3종목 (prism-insight 검증 규칙)
- 리포트마다 전략별 성과 리더보드 포함 → "어떤 전략이 나한테 맞는가" 데이터 축적

## 6. Q&A 설계 (투자근거 질문답변)

텔레그램 대화형 명령:
- `/why <종목>` — 최근 판단의 근거 전문 (토론 로그 + 데이터 스냅샷)
- `/prob <종목>` — 확신도 산출 내역 (Bull/Bear 논점별 가중치)
- `/report` — 최신 리포트 재전송, `/history <종목>` — 과거 판단 이력과 적중 여부
- 자유 질문 — RAG: 리포트 아카이브(FTS5+임베딩) 검색 → 근거 인용 답변

확신도(확률) 산출 정책:
- LLM 자체 확신도 + 과거 유사 판단의 실제 적중률(캘리브레이션) 결합
- 판단 이력이 쌓이면 "이 확신도 구간의 실제 승률" 표기 (정직한 확률)

## 7. MVP 로드맵

| Phase | 목표 | 산출물 |
|---|---|---|
| **P1** (1-2주) | KR 데이터 파이프라인 + 장전/마감 텔레그램 리포트 (룰 기반 + 단일 LLM 요약) | 매일 아침 리포트가 도착하는 경험 |
| **P2** (2-3주) | 멀티에이전트 두뇌 이식 — 주목 종목 추천 + 근거/확신도 카드 | TradingAgents 파이프라인 KR 적응 |
| **P3** (2주) | 전략 시뮬레이션 3종 + 성과 리더보드 | 가상 포트폴리오 DB + 리포트 통합 |
| **P4** (2주) | US 시장 확장 + Q&A 봇 (/why, /prob, 자유질문 RAG) | 24시간 커버 |
| **P5** (2주+) | 웹 대시보드 (리포트 아카이브·전략 비교 차트) + 반성 루프 | Next.js + FastAPI |

## 8. 기술 스택

- **Python 3.11+**, LangGraph(TradingAgents 차용), cluefin-openapi/cluefin-ta(pip), pykrx, yfinance
- python-telegram-bot v20+, APScheduler, FastAPI, SQLite(+FTS5), Next.js(P5)
- LLM: 프로바이더 추상화 유지(TradingAgents 레지스트리 그대로) — 딥씽킹/퀵씽킹 2-티어
- 예상 LLM 비용(개인용): 일 4회 리포트 × 에이전트 파이프라인 ≈ **월 $30~100** (모델 티어에 따라)

## 9. 리스크 & 정책

- **코드 출처 추적 규칙**: AGPL 계열(prism-insight, FinceptTerminal)에서 코드를 참고/차용한 파일에는 헤더 주석 `# PROVENANCE: <repo>/<path> (AGPL — rewrite before service launch)` 기록 + 루트 `PROVENANCE.md`에 목록 유지. 서비스화 전 이 목록만 재작성하면 되게
- 개인 사용 중에도 **리포 코드를 외부에 재배포하지 않음** (블로그 공유 등 시 라이선스 재확인)
- 투자 판단 책임 고지 문구 리포트 하단 고정
- API 키 관리: .env, 저장소 커밋 금지
- KIS/키움 API는 조회 전용으로 시작 (주문 모듈 미연결 = 시뮬레이션만)

---

## 10. 서비스화 아키텍처 (웹/앱 다중 사용자)

### 10.1 핵심 설계 원칙: 마스터-개인화 팬아웃 (1×N)

LLM 비용이 사용자 수에 비례하면 서비스가 성립하지 않는다. 콘텐츠를 2계층으로 분리:

| 계층 | 내용 | 생성 비용 | 예 |
|---|---|---|---|
| **마스터 리포트** (공유) | 증시현황, 레짐 판정, 주목 종목 카드, 전략 리더보드 | **1회/스케줄** — 사용자 수와 무관 (고성능 모델) | 장전/마감 브리핑 본문 |
| **개인화 레이어** (사용자별) | 내 관심종목 코멘트, 내 가상 포트폴리오 진단, Q&A | 사용자당 소량 토큰 (경량 모델) + 쿼터제 | "회원님 관심종목 중 2개가 오늘 주목 종목에 포함" |

→ 멀티에이전트 파이프라인(고비용)은 하루 4회 고정 실행, 개인화는 마스터 리포트를 입력으로 받는 짧은 경량 호출. prism-insight가 450명을 월 $310에 운영하는 것이 이 모델의 실증.

### 10.2 서비스 아키텍처

```
[클라이언트]   웹(Next.js/PWA) · 앱(React Native, 2단계) · 텔레그램 봇
                    │  HTTPS / WSS / 푸시(FCM·APNs)
[게이트웨이]   FastAPI — Auth(JWT/소셜로그인) · 구독/결제 · 레이트리밋
                    │
[서비스 API]   리포트 조회 │ 관심종목/포트폴리오 │ Q&A(쿼터) │ 시뮬레이션 성과
                    │                    ▲
[비동기 워커]  (Redis Queue/Celery)      │
   ① 분석 파이프라인(공유·스케줄) ──▶ 마스터 리포트 생성 (멀티에이전트)
   ② 개인화 워커 ──▶ 마스터 × 사용자 컨텍스트 → 개인 섹션 (경량 LLM, 팬아웃)
   ③ 배포 워커 ──▶ 푸시/텔레그램/(이메일) 팬아웃, 사용자별 전송상태 추적
                    │
[데이터]      PostgreSQL(+pgvector: 리포트 임베딩) · Redis(캐시·큐·세션)
              S3호환(PDF/차트 이미지) · 시세 캐시
```

동작 흐름 (마감 브리핑 예):
1. 스케줄러가 16:00 `market_close_kr` 이벤트 발행
2. 분석 파이프라인: 데이터 수집 → 멀티에이전트 토론 → 마스터 리포트 저장 (+판단 로그)
3. 개인화 워커: 구독자별 관심종목/포트폴리오 매칭 → 개인 섹션 생성 (배치)
4. 배포 워커: 티어별로 푸시·텔레그램 팬아웃 (실패 재시도, 멱등)
5. Q&A는 상시: 질문 → RAG(마스터 리포트+판단 로그 검색) → 근거 인용 답변

### 10.3 티어 설계 (예시)

| | Free | Basic | Pro |
|---|---|---|---|
| 마감 요약 리포트 | ○ (지연/축약) | ○ 실시간 전문 | ○ |
| 장전 브리핑·주목 종목 카드 | 1개만 | 전체 | 전체 |
| Q&A (/why, 자유질문) | 3회/일 | 30회/일 | 사실상 무제한 |
| 관심종목 개인화 | ✗ | 10종목 | 무제한+가상 포트폴리오 진단 |
| 전략 시뮬레이션 | 열람만 | 열람 | 커스텀 전략 생성 |

### 10.4 ⚠️ 규제 검토 (한국 — 서비스화 전 필수)

- **유사투자자문업 신고**: 불특정 다수에게 대가를 받고 투자 정보/조언 제공 시 금융위 신고 대상. 방송형 리포트 서비스가 여기 해당할 가능성 높음
- **투자자문업 등록**: 특정인 대상 1:1 맞춤 투자 판단 자문은 별도 등록 요건(자본금 등) — **개인 포트폴리오 "진단·권고" 기능은 경계 영역**
- 설계상 대응: ① 리포트는 "정보 제공" 프레임(매수 권유 표현 지양) ② Q&A는 "AI 판단의 근거 설명"으로 한정 ③ 개인화는 "관심종목 필터링" 수준부터 시작 ④ 면책 고지 상시 노출 — **출시 전 법률 자문 필수**

### 10.5 단계별 인프라 전개

| 단계 | 규모 | 인프라 | 비고 |
|---|---|---|---|
| Stage 0 | 개인 (P1~P5) | 로컬/VPS 1대 + SQLite | 현재 계획 |
| Stage 1 | 베타 ~500명 | VPS 1~2대(도커 컴포즈) + Managed Postgres + Redis | 텔레그램+웹 중심, 결제는 수동/토스페이먼츠 |
| Stage 2 | ~수천 명 | 워커 분리·큐 기반 오토스케일, RN 앱 출시(FCM) | Q&A 캐싱, 임베딩 검색 pgvector→전용화 검토 |
| Stage 3 | 만 명+ | 멀티리전, 실시간 알림 스트림, 데이터 파이프라인 분리 | 이 시점엔 재설계 여력 있음 |

### 10.6 개인 MVP → 서비스 마이그레이션을 쉽게 하는 P1 설계 규칙 (→ §12.3에 후속 반영)

서비스화를 염두에 두고 **P1부터 지킬 것** (비용은 거의 안 들고 나중에 크게 이득):
1. **저장소 추상화**: DB 접근을 repository 패턴으로 — SQLite→Postgres 교체가 코드 1곳
2. **마스터/개인 분리를 지금부터**: 리포트 생성 함수가 "마스터 생성"과 "개인 섹션 생성"을 처음부터 분리 (개인용에선 사용자가 1명일 뿐)
3. **배포 채널 추상화**: `Notifier` 인터페이스(텔레그램 구현체 1개로 시작) — 푸시/이메일은 구현체 추가
4. **판단 로그 스키마를 서비스 기준으로**: decision_id, 근거 구조화, 데이터 스냅샷 — Q&A와 캘리브레이션의 원천이자 서비스의 핵심 자산
5. **이벤트 기반 스케줄링**: cron이 파이프라인을 직접 호출하지 말고 이벤트 발행 → 핸들러 구조

---

## 11. 데이터 레이어 확정 설계 (open-trading-api 분석 반영)

### 11.1 핵심 발견: KIS 하나로 KR+US 시세 커버 가능

- **미국 실시간 시세가 무료·0분 지연** (KIS WebSocket `delayed_ccnl` — 홍콩/중국/일본만 15분 지연). 미국 현재가·순위·분봉 REST도 제공 → **US 시세를 위해 유료 API 불필요**
- 국내: 현재가·일/분봉·호가·**투자자별 매매동향(외인/기관)**·업종지수·순위분석(거래량/등락률/시총)·프로그램매매·신용/공매도·예탁원 권리정보(배당/증자)·조건검색까지 156개 엔드포인트
- 모의투자 계정에서도 시세 조회(F로 시작하는 TR) 대부분 동작. 단 **모의 계정은 레이트리밋이 낮음 → 리포트 배치는 실전 계정 키 사용** (조회만 하므로 안전)

### 11.2 실전 제약 (설계에 반영)

| 제약 | 값 | 대응 |
|---|---|---|
| 액세스 토큰 | 1일 유효, 발급 시 카카오 알림 | 파일 캐싱 후 재사용 (공식 `kis_auth.py` 패턴) — 일 1회 발급 |
| REST 레이트리밋 | 실전 ~20 req/s, 모의 대폭 낮음 (`EGW00201`) | 토큰버킷 리미터 내장, 배치 간 smart_sleep |
| WebSocket 구독 | 최대 40개 동시 | 관심종목+지수 우선순위 큐 |
| US 일봉 | `inquire_daily_chartprice`는 지수구성종목 한정 | 광범위 심볼은 `dailyprice` 사용 |

### 11.3 데이터 소스 최종 매트릭스

| 데이터 | 소스 | 비고 |
|---|---|---|
| KR 시세·수급·순위·실시간 | **KIS API** (공식 샘플 래핑) | 실전 계정 키, 조회 전용 |
| KR 과거 데이터 백필 (백테스트용) | **pykrx** | 무인증·무제한, KIS 페이지네이션 부담 제거 |
| KR 공시·재무제표 심층 | **OpenDART** (+cluefin-xbrl 파서) | KIS는 요약 재무만 제공 |
| US 시세·실시간 | **KIS API** | 무료 0분 지연 |
| US 펀더멘털·실적 | **yfinance** (→FMP 확장) | KIS는 US 재무 미제공 |
| 뉴스 본문 | 웹검색/RSS (KIS는 제목만) | 뉴스 에이전트 입력 |
| 매크로 | FRED(미) + ECOS(한은) | 레짐 판정 입력 |

### 11.4 구현 지침

- 공식 `examples_user/kis_auth.py`를 베이스로 **자체 typed client 래핑**: 토큰 캐시, 재시도, 구조화 로깅, .env 시크릿 (평문 토큰파일 개선), DataFrame 정규화
- cluefin KIS 클라이언트 사용 시 공식 리포의 TR ID/파라미터로 검증 (공식 리포 = 스펙의 기준)
- 공식 리포는 LICENSE 없음(참고용 고객 제공 코드) → 개인 사용 문제없음, 서비스화 시 재배포 대신 자체 구현 유지. MCP Code Assistant 부분만 MIT

## 12. FinceptTerminal에서 가져올 설계 패턴 (클린룸 재구현)

> ⚠️ AGPL+상용 이중 라이선스: 개인 학습은 명시 허용 → **개인 단계에서는 코드까지 참고 OK** (§9 출처 추적 필수). 단 모든 비즈니스/SaaS 이용에 상용 라이선스 강제(위반 시 연 $50k 배상 조항) → **서비스화 시 아래 항목은 클린룸 재작성**

1. **Alpha Arena 패턴 → 우리 전략 시뮬레이션의 설계도**: 동일 시장·동일 틱에 N개 전략(LLM 에이전트)을 경쟁시키고, 모든 틱·프롬프트·판단·주문·체결·PnL을 SQL 테이블에 기록 → **임의 시점 재현 가능한 감사 추적**. 우리 Q&A(`/why`)가 "그때 그 판단의 정확한 근거"를 보여줄 수 있는 기반. 리더보드·모델별 대화 로그·리스크 게이트·서킷브레이커(파싱 3회 실패/드로다운 50% 시 중단) 포함
2. **스케줄러 문법**: `daily HH:MM`, `weekday HH:MM`, `every Nh` + **장 시간에만 발동하는 market-hours cron 트리거** — SQLite 단일 테이블, next_run_at 기준 틱
3. **알림 프로바이더 추상화**: 17개 채널을 단일 인터페이스로 — 우리 `Notifier` 설계 검증 (텔레그램 구현체부터)
4. **12컬럼 대시보드 그리드 + 역할별 프리셋 템플릿 + 위젯 카탈로그**: P5 웹 대시보드에서 `{widget_id, x, y, w, h}` 그리드 스키마 + 프리셋(예: "리포트 중심"/"시뮬레이션 중심"/"매크로 중심") 채택. 참고 위젯 분류: 포트폴리오 요약/성과/리스크지표/오늘손익/보유종목/관심종목/섹터히트맵/스파크라인/급등락/경제캘린더/뉴스
5. **DataHub 패턴**: 한 번 fetch → 다중 구독자 pub/sub (topic: `domain:subdomain:id`) + TTL 캐시 — 리포트 파이프라인·Q&A·대시보드가 같은 시세 fetch를 공유

## 13. 종목 지식 저장소 — RAG식 분석 캐시 (2026-07-19 추가, 구현됨)

> 아이디어: 종목 분석 결과를 팩트 단위로 청킹해 저장 → 다음 논의 때 "이미 아는 것"은 주입하고 델타만 새로 분석. prism-insight 인사이트 에이전트(Mem0 스타일)로 검증된 패턴.

**핵심 원칙: "비싼 추론은 캐시하고, 싼 데이터는 캐시하지 않는다"**
- 캐시 대상: LLM이 생성한 분석 (사업 구조, 투자 논지, 리스크, 판단 결과)
- 캐시 금지: 시세·수급 원본 (API 한 번이면 신선 — 항상 data/에서 라이브 조회)

**시효(TTL) 관리 — 팩트 유형별 유통기한** (`knowledge/store.py TTL_DAYS`):
profile 365d · business 180d · financials 120d(분기) · risk 90d · thesis 60d · catalyst 30d · technicals 5d · flow 3d · **outcome(판단 결과) 무기한**

**동작 방식** (`002_stock_knowledge.sql`, `src/aim/knowledge/`):
- 같은 (symbol, fact_type, topic)은 최신 팩트가 이전 것을 **대체(supersede)** — 삭제 대신 이력 보존
- 모든 팩트에 as_of(기준 시점)·출처(report/decision FK) — LLM 프롬프트에 신선도 항상 노출
- 만료 팩트는 조회 시 기본 제외 (`include_stale`로 열람 가능)
- 검색: FTS5 (v1, 한국어 형태소 한계 수용) → P4에서 임베딩 리랭크 추가 (embedding 컬럼 예약)

**활용 지점**:
- P2 두뇌: 분석 전 `get_context(symbol)` 주입 → 토큰 절감 + 판단 일관성
- P4 Q&A: 자유질문의 1차 검색 소스
- 반성 루프: outcome 팩트 축적 = 확률 캘리브레이션 원천

## 14. 관심종목 실시간 추적·시그널 (2026-07-19 추가, ① 구현됨)

> 관심종목의 공시·거래대금·거래량을 상시 추적해 룰 기반 시그널 생성. prism-insight 트리거 개념의 상시화.

**데이터 경로**: 시세 = KIS REST 폴링 30초(40종목에 4req/s, 여유) → WS 업그레이드 여지 / 공시 = OpenDART 1분 폴링(푸시 없음, 일 20k콜 내 여유), 07:00~19:00 가동
**핵심 룰**: VOLUME_SURGE(동시각 누적거래량 z>3 — **시간대 정규화 필수**, volume_baselines 테이블), PRICE_MOVE(5분 ±3%), DISCLOSURE(제목 키워드 → 카테고리·중요도 자동 분류), **COMBO**(서지+공시 ±30분 = critical)
**원칙**: 감지는 100% 룰(비용 제로) / LLM은 시그널 발생 시 해석 코멘트만(P2+) / 시그널 → knowledge catalyst 팩트 자동 축적 / DB 기반 쿨다운 30분(재알림 억제) / 시그널 이력 = 적중률 캘리브레이션·모멘텀 전략 진입신호 원천(P3)
**구현**: `src/aim/watch/` + `003_watchlist.sql` + CLI(`aim watchlist add/list/rm`, `aim watch --mock`).
**② 완료 (OpenDART 폴러, `watch/dart.py` + `004_dart_seen.sql`)**: list.json 당일 최신순 페이징, rcept_no 중복제거(dart_seen, 7일 프루닝), 기처리 접수번호 도달 시 조기종료(평시 1콜/폴링), prime()으로 기동 시 알림 폭주 방지, 쿼터(020)·네트워크 오류 격리. `aim watch` = 공시 전용 실전 모드(NullIntradayProvider, 최소 60초 간격, 창 07:00~19:00). 유의: list API는 접수 '일자'만 제공(시각 없음).
남은 것: ③ KIS 폴링 프로바이더 + pykrx 분봉 baseline 백필 + 야간 baseline 갱신 배치
